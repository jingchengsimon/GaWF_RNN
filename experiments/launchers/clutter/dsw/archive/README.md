# DSW campaign launcher snapshots

These files preserve the exact orchestration used for the 2026-09-22 DSW campaigns. They are
provenance snapshots, not reusable launchers. Paths, ports, GPU ownership, result roots, and
process state must be revalidated before any future use. The active pause trigger is one directory
above and reads its machine-specific connection details from an ignored local config.
